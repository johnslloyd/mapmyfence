## Packages
leaflet | Core mapping library
react-leaflet | React components for Leaflet maps
@types/leaflet | TypeScript definitions for Leaflet
framer-motion | Smooth animations for UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Leaflet CSS must be imported in index.css
Map requires height to be set explicitly
Address lookup might require an external geocoding service (mocked for now)
